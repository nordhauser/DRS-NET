namespace DungeonRunners.Core
{
    public enum ResolutionSource
    {
        Client,
        ConstructorDefault,
        AuthoredDefault,
        CompatibilityFallback,
        Unknown,
        Blocked
    }
}
